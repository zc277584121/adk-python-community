name: Python Unit Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Test against all Python versions supported in pyproject.toml
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: |
          uv venv .venv
          source .venv/bin/activate
          # Install dependencies as recommended in CONTRIBUTING.md
          uv sync --extra test --extra milvus

      - name: Run unit tests with pytest
        run: |
          source .venv/bin/activate
          # Run unit tests plus deterministic Milvus Lite coverage.
          pytest tests/unittests \
            tests/integration/memory/test_milvus_memory_service_e2e.py::test_milvus_lite_memory_e2e \
            tests/integration/tools/test_milvus_toolset_e2e.py::test_milvus_lite_toolset_e2e
